{
    // 最外层对象，必须包含 "legacies" 数组
    // 一个文件可以定义多个职业，每个职业是数组中的一项
    "legacies": [
        {
            // 【必填】职业的唯一标识符，全部小写字母 + 下划线，不可重复
            // 你的前缀 "zb_" 可以避免与其他模组的 id 冲突，是好习惯
            "id": "zb_nobody",

            // 【必填】职业图标文件名（不含 .png 后缀）
            // 图片必须放在 模组/images/legacies/ 文件夹下，命名为 Nobody.png
            // 大小建议 128x128 像素，格式 PNG
            "image": "Nobody",

            // 【必填】在选择职业界面上显示的名称
            // 如果游戏内显示为白框，请使用本地化文件（loc）解决
            "label": "失意青年",

            // 【必填】鼠标悬停在职业卡上时显示的描述文本
            "description": "我的身上并无可圈可点之处，但一句“我还活着”，便足以证明一切。",

            // 【必填】点击选择该职业后，弹出的确认窗口中的详细描述
            // 通常用于营造角色背景和氛围
            "startdescription": "自大学毕业，我已然工作了5个年头，这座钢铁巨兽每天将我囫囵吞下，吮吸殆尽后吐在被褥之间。5年的光阴彻底浇熄了我的梦想。",

            // 【必填】开局时桌面上默认存在的行动框（动词）id
            // "work" 是原版的工作框，你也可以换成自己定义的动词 id
            "startingVerbId": "work",

            // 【可选】布尔值，true 表示该职业可以手动选择开始新游戏
            // 如果设为 false，这个职业只能作为“继承职业”出现（需配合 fromEnding）
            "newstart": true,

            // 【关键字段】布尔值，true 表示无论上一局结局如何，此职业都会显示在可选列表
            // 如果缺失此字段，默认为 false，导致职业被隐藏（你之前遇到的问题）
            "availableWithoutEndingMatch": true,

            // 【必填】开局时直接发放给玩家的卡牌及数量
            // 格式为 "卡牌id": 数量
            // 这里的 id 必须是在游戏本体或模组中已定义的卡牌
            "effects": {
                "health": 2,    // 健康，原版 id
                "reason": 2,    // 理性，原版 id
                "passion": 2,   // 激情，原版 id
                "funds": 3      // 资金，原版 id
            },

            // 【必填】屏幕底部状态栏显示的四个元素 id
            // 数组长度必须恰好为 4，不能多也不能少
            // 如果只想跟踪少于四个，可以用重复的 id 补齐
            "statusbarelements": [
                "health",
                "reason",
                "passion",
                "funds"
            ]

            // ========== 以下是你当前未使用但常用的可选字段 ==========
            // "fromEnding": "ending_id"
            // 当 availableWithoutEndingMatch 为 false 时，需指定解锁此职业的前置结局

            // "excludesOnEnding": ["legacy_id_1", "legacy_id_2"]
            // 当前职业结束后，禁止某些职业出现在继承选项中

            // "tablecoverimage": "图片名"
            // 自定义桌面背景图，图片放在 images/ui/ 下

            // "statusbarelements" 的变体写法：
            // 如果不想直接显示数量，可以写成 "element_id:icon" 的形式更换图标
        }
    ]
}